<%- include('../partials/header/header.ejs'); %>

<% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
  <%- include('../partials/menu/menuAdmin') %>
<% } else if (user && user.role === 'teacher') { %>
  <%- include('../partials/menu/menuTeacher') %>
<% } else if (user && user.role === 'player') { %>
  <%- include('../partials/menu/menuPlayer') %>
<% } else { %>
  <%- include('../partials/menu/menuGuest') %>
<% } %>

<div class="containerSQ">
  <div class="d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card p-4 shadow-lg" style="background-color: #0033a0; border: 3px solid #f0ec19; width: 100%; max-width: 500px;">
      <h2 class="text-center mb-4" style="color: #f0ec19; font-weight: bold;">🎮 Login no SaberQuest</h2>

      <form action="/login" method="POST">
        <div class="mb-3">
          <label class="form-label text-light">Usuário:</label>
          <input type="text" name="username" required class="form-control border-success" />
        </div>

        <div class="mb-3">
          <label class="form-label text-light">Senha:</label>
          <input type="password" name="password" required class="form-control border-success" />
        </div>

        <div class="d-grid gap-2 mt-4">
          <button type="submit" class="btn btn-success btn-lg">
            Entrar no Jogo
          </button>
        </div>

        <div class="mt-4 text-center">
          <a href="/" class="text-light me-3">
            <i class="bi bi-house-fill"></i> Voltar para Home
          </a>
          |
          <a href="/register" class="text-warning ms-3">
            <i class="bi bi-person-plus-fill"></i> Criar Conta
          </a>
        </div>
      </form>
    </div>
  </div>
</div>

<%- include('../partials/footer/footer.ejs'); %>
